# Vehicle Silhouette Classification — ML Case Study (scikit-learn)

Work in progress. This repository will include EDA, supervised classification, clustering + PCA, and final conclusions.

## Tech stack
- Python 3.12
- pandas, numpy
- scikit-learn
- matplotlib / seaborn
- Jupyter

## Repository structure
- `notebooks/` — EDA + modeling notebooks
- `data/` — notes and small samples (full dataset kept local)
- `images/` — key charts
- `docs/` — short write-ups
